package main

import "fmt"

const NAME  = "book"

var (
    v1 int
    v2 string
)
var mainName  = "main name"
func main() {
    fmt.Print(v1,"111\n")
    fmt.Print(v2,"222")

   // fmt.Println("learn1")
   //fmt.Print(mainName)
   //fmt.Print(NAME)
}
